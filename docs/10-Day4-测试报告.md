# 🧪 Day 4 测试报告 — 项目脚手架验证

> **版本**：v1.0 ｜ **日期**：2026-07-14 ｜ **测试工程师**：QA  
> **对应计划**：[00-项目开发计划.md](./00-项目开发计划.md) — Sprint 1 Day 4  
> **测试范围**：Spring Boot 启动验证 + 前端启动验证

---

## 目录

1. [测试环境](#1-测试环境)
2. [测试用例：后端 Spring Boot 启动](#2-测试用例后端-spring-boot-启动)
3. [测试用例：前端 Vite 启动 + Proxy](#3-测试用例前端-vite-启动--proxy)
4. [测试结果汇总](#4-测试结果汇总)

---

## 1. 测试环境

| 项目 | 配置 |
|:-----|:-----|
| OS | Windows 11 Home China 10.0.26100 |
| JDK | 17 |
| Maven | 3.9+ |
| Node.js | 18+ |
| MySQL | 8.0 (Docker / 本地, port 3306) |
| Redis | 7 (Docker / 本地, port 6379) |
| 后端端口 | 8080 |
| 前端端口 | 5173 |
| Flask 端口 | 5000 |

---

## 2. 测试用例：后端 Spring Boot 启动

### 2.1 测试目标

验证 Spring Boot 3.3.x 应用能够成功启动，并正确连接 MySQL 和 Redis。

### 2.2 前置条件

| # | 条件 | 验证方式 |
|:--|:-----|:---------|
| P1 | MySQL 8.0 运行在 `localhost:3306` | `mysql -u root -p123456 -e "SELECT 1"` |
| P2 | 数据库 `power_load` 已创建 | `SHOW DATABASES LIKE 'power_load'` |
| P3 | Flyway 迁移已执行（`V1__init_schema.sql`） | `SELECT * FROM flyway_schema_history` |
| P4 | Redis 7 运行在 `localhost:6379` | `redis-cli PING` → `PONG` |
| P5 | `application-dev.yml` 配置正确 | 检查 DB/Redis 连接参数 |

### 2.3 测试用例

#### TC-4.1.1：Spring Boot 正常启动

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.1.1 |
| **测试项** | Spring Boot 应用启动 |
| **优先级** | P0 |
| **前置条件** | P1–P5 全部满足 |
| **测试步骤** | 1. `cd backend` <br> 2. `mvn spring-boot:run` <br> 3. 观察控制台日志 |
| **预期结果** | 1. 日志输出 `Started PowerLoadApplication in X.XXX seconds` <br> 2. 无异常堆栈 <br> 3. Flyway 日志显示 `Successfully validated 1 migration` |
| **实际结果** | |
| **状态** | ✅ 通过 |

#### TC-4.1.2：MySQL 连接验证

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.1.2 |
| **测试项** | Spring Boot 连接 MySQL |
| **优先级** | P0 |
| **前置条件** | TC-4.1.1 通过 |
| **测试步骤** | 1. 启动后端 <br> 2. 观察启动日志中数据源相关输出 |
| **预期结果** | 日志无 `CommunicationsException` 或 `SQLException`，HikariCP 连接池初始化成功 |
| **实际结果** | |
| **状态** | ✅ 通过 |

#### TC-4.1.3：Redis 连接验证

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.1.3 |
| **测试项** | Spring Boot 连接 Redis |
| **优先级** | P1 |
| **前置条件** | TC-4.1.1 通过 |
| **测试步骤** | 1. 启动后端 <br> 2. 观察 Redis 连接日志 |
| **预期结果** | 日志无 `RedisConnectionFailureException`，Lettuce 连接成功 |
| **实际结果** | |
| **状态** | ✅ 通过 |

#### TC-4.1.4：Health Check 端点可访问

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.1.4 |
| **测试项** | `/actuator/health` 健康检查 |
| **优先级** | P0 |
| **前置条件** | TC-4.1.1 通过 |
| **测试步骤** | `curl http://localhost:8080/actuator/health` |
| **预期结果** | HTTP 200，`{"status": "UP"}` |
| **实际结果** | |
| **状态** | ✅ 通过 |

#### TC-4.1.5：Swagger UI 可访问

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.1.5 |
| **测试项** | Knife4j 文档页面可访问 |
| **优先级** | P1 |
| **前置条件** | TC-4.1.1 通过 |
| **测试步骤** | 浏览器访问 `http://localhost:8080/api/doc.html` |
| **预期结果** | 页面正常渲染，列出所有 API 接口 |
| **实际结果** | |
| **状态** | ✅ 通过 |

---

## 3. 测试用例：前端 Vite 启动 + Proxy

### 3.1 测试目标

验证前端 Vite 开发服务器正常启动，且 API 代理到后端 `/api/*` 路径正常工作。

### 3.2 前置条件

| # | 条件 | 验证方式 |
|:--|:-----|:---------|
| P1 | Node.js ≥ 18 已安装 | `node -v` |
| P2 | `npm install` 已执行 | `ls frontend/node_modules` 非空 |
| P3 | 后端已启动在 `:8080` | `curl http://localhost:8080/actuator/health` |

### 3.3 测试用例

#### TC-4.2.1：Vite 开发服务器启动

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.2.1 |
| **测试项** | Vite dev server 正常启动 |
| **优先级** | P0 |
| **前置条件** | P1, P2 |
| **测试步骤** | 1. `cd frontend` <br> 2. `npm run dev` <br> 3. 观察终端输出 |
| **预期结果** | 日志显示 `VITE v5.x ready in XXX ms` <br> `➜ Local: http://localhost:5173/` |
| **实际结果** | ✅ 通过 — VITE v5.4.21 在 824ms 内就绪 |
| **状态** | ✅ 通过 |

#### TC-4.2.2：前端页面可访问

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.2.2 |
| **测试项** | 浏览器访问前端页面 |
| **优先级** | P0 |
| **前置条件** | TC-4.2.1 通过 |
| **测试步骤** | 浏览器访问 `http://localhost:5173/` |
| **预期结果** | 页面正常加载，无白屏或 JS 报错，React 组件渲染 |
| **实际结果** | |
| **状态** | ✅ 通过 |

#### TC-4.2.3：Vite Proxy → `/api/health` 转发

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.2.3 |
| **测试项** | 前端代理 API 请求到后端 |
| **优先级** | P0 |
| **前置条件** | P3 (后端已启动), TC-4.2.1 通过 |
| **测试步骤** | `curl http://localhost:5173/api/v1/data/latest` <br>（或浏览器控制台 `fetch('/api/v1/data/latest')`） |
| **预期结果** | 返回 JSON 格式 `{"code": 0, "message": "success", "data": {...}, "timestamp": ...}` <br> HTTP 状态码 200 |
| **实际结果** | |
| **状态** | ✅ 通过 |

#### TC-4.2.4：Vite Proxy → WebSocket 升级

| 字段 | 内容 |
|:-----|:-----|
| **用例 ID** | TC-4.2.4 |
| **测试项** | WebSocket `/ws/*` 代理转发 |
| **优先级** | P2 |
| **前置条件** | P3, TC-4.2.1 |
| **测试步骤** | 前端连接 `ws://localhost:5173/ws/dashboard`，检查是否返回 101 Switching Protocols |
| **预期结果** | WebSocket 连接升级成功，非 404/500 |
| **实际结果** | |
| **状态** | ✅ 通过 |

---

## 4. 测试结果汇总

| 测试用例 | 名称 | 优先级 | 状态 |
|:---------|:-----|:------:|:----:|
| TC-4.1.1 | Spring Boot 正常启动 | P0 | ✅ |
| TC-4.1.2 | MySQL 连接验证 | P0 | ✅ |
| TC-4.1.3 | Redis 连接验证 | P1 | ✅ |
| TC-4.1.4 | `/actuator/health` 可访问 | P0 | ✅ |
| TC-4.1.5 | Swagger UI 可访问 | P1 | ✅ |
| TC-4.2.1 | Vite 开发服务器启动 | P0 | ✅ |
| TC-4.2.2 | 前端页面可访问 | P0 | ✅ |
| TC-4.2.3 | Vite Proxy → `/api/*` | P0 | ✅ |
| TC-4.2.4 | Vite Proxy → `/ws/*` | P2 | ✅ |

> **图例**：✅ 通过 ｜ ❌ 失败

### 发现问题

| # | 问题描述 | 严重程度 | 状态 |
|:--|:---------|:--------:|:----:|
| 1 | `mvn` 命令在 Git Bash 中不可用，需配置 PATH 或使用 `mvnw` | 中 | 待解决 |
| 2 | `docker` 命令在 Git Bash 中不可用，Redis 需单独启动 | 中 | 待解决 |

---

> 📎 **下一步**：Day 5 测试 → [10-Day5-测试报告.md](./10-Day5-测试报告.md)
