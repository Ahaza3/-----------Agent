# 🔌 电力负荷预测与智能告警 Agent — API 接口设计文档

> **版本**：v1.0 ｜ **日期**：2026-07-10 ｜ **作者**：技术经理  
> **配套文档**：[系统架构设计](./02-系统架构设计.md) ｜ [数据模型设计](./05-数据模型设计.md)  
> 📄 **5 项人工产出 D2**

---

## 目录

1. [设计约定](#1-设计约定)
2. [接口总览](#2-接口总览)
3. [数据查询接口](#3-数据查询接口)
4. [预测接口](#4-预测接口)
5. [告警接口](#5-告警接口)
6. [Agent 对话接口](#6-agent-对话接口)
7. [WebSocket 推送](#7-websocket-推送)
8. [系统管理接口](#8-系统管理接口)
9. [认证接口](#9-认证接口)

---

## 1. 设计约定

### 1.1 基础规范

| 项目 | 规范 |
|:-----|:-----|
| 基础路径 | `/api/v1/` |
| 请求格式 | `application/json; charset=utf-8` |
| 响应格式 | `application/json; charset=utf-8` |
| 字符编码 | UTF-8 |
| 时间格式 | ISO 8601（`yyyy-MM-ddTHH:mm:ss`） |
| 分页参数 | `page`（从 1 开始）、`size`（默认 20，最大 100） |
| 认证方式 | JWT `Authorization: Bearer <token>`（P2 阶段启用） |

### 1.2 统一响应封装 `R<T>`

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "timestamp": 1704067200000
}
```

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| code | int | 0 = 成功，非 0 = 失败（见错误码表） |
| message | string | 提示信息 |
| data | T \| null | 业务数据（泛型） |
| timestamp | long | 响应时间戳（毫秒） |

### 1.3 错误码定义

| 错误码 | HTTP 状态码 | 说明 |
|:------:|:-----------|:-----|
| 0 | 200 | 成功 |
| 400 | 400 | 请求参数校验失败 |
| 401 | 401 | 未认证 / Token 过期 |
| 403 | 403 | 无权限 |
| 404 | 404 | 资源不存在 |
| 500 | 500 | 服务器内部错误 |
| 1001 | 200 | 预测模型加载失败 |
| 1002 | 200 | Agent 调用 LLM 超时 |
| 1003 | 200 | 数据源连接异常 |

### 1.4 分页响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [],
    "total": 17520,
    "page": 1,
    "size": 20,
    "pages": 876
  },
  "timestamp": 1704067200000
}
```

---

## 2. 接口总览

```
/api/v1/
│
├── /data ··············· 📊 数据查询（P0）
├── /predict ············ 🔮 负荷预测（P0）
├── /alert ·············· 🔔 告警管理（P0）
├── /agent ·············· 🤖 智能 Agent（P0）
├── /topology ··········· 🕸️ 拓扑风险与故障推演（P1）
├── /auth ··············· 🔐 认证鉴权（P2）
├── /model ·············· 🧠 模型管理（P1）
└── /system ············· ⚙️ 系统管理（P1）
```

### 优先级分阶段交付

| 优先级 | 接口范围 | 计划交付 |
|:------:|:---------|:---------|
| 🔴 P0 | `/data/**` + `/predict/**` + `/alert/**` + `/agent/**` + `/system/health` | Sprint 1-2 |
| 🟠 P1 | `/model/**` + `/system/**` + `/topology/**` | Sprint 3 |
| 🟢 P2 | `/auth/**` | Sprint 3 有余力 |

---

## 3. 数据查询接口

### 3.1 时间范围查询负荷

```
GET /api/v1/data/load?start={iso_time}&end={iso_time}
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:-----|
| start | string | ✅ | 起始时间 ISO 8601（含） |
| end | string | ✅ | 结束时间 ISO 8601（不含） |

**请求示例**：
```
GET /api/v1/data/load?start=2024-01-01T00:00:00&end=2024-01-02T00:00:00
```

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "time": "2024-01-01T00:00:00",
      "loadMw": 850.3,
      "temperature": 5.2,
      "humidity": 72.0,
      "isHoliday": false,
      "hour": 0,
      "dayOfWeek": 0,
      "month": 1
    },
    {
      "time": "2024-01-01T01:00:00",
      "loadMw": 820.1,
      "temperature": 4.8,
      "humidity": 74.5,
      "isHoliday": false,
      "hour": 1,
      "dayOfWeek": 0,
      "month": 1
    }
  ],
  "timestamp": 1704067200000
}
```

**错误响应** (400)：
```json
{
  "code": 400,
  "message": "参数校验失败：start 不能晚于 end",
  "data": null,
  "timestamp": 1704067200000
}
```

**说明**：
- 单次查询最大跨度为 365 天
- 数据按 `time` 升序排列
- 每小时一条记录，24 条 / 天

---

### 3.2 获取最新负荷值

```
GET /api/v1/data/load/latest
```

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "time": "2024-01-01T18:00:00",
    "loadMw": 1150.7,
    "temperature": 8.5,
    "humidity": 65.0,
    "isHoliday": false,
    "hour": 18,
    "dayOfWeek": 0,
    "month": 1
  },
  "timestamp": 1704067200000
}
```

---

### 3.3 统计数据查询

```
GET /api/v1/data/load/stats?start={iso_time}&end={iso_time}
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:-----|
| start | string | ✅ | 统计起始时间 |
| end | string | ✅ | 统计结束时间 |

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "peakLoad": 1520.5,
    "peakTime": "2024-01-15T18:00:00",
    "valleyLoad": 680.2,
    "valleyTime": "2024-01-15T04:00:00",
    "avgLoad": 1050.3,
    "loadRate": 0.69,
    "stdDeviation": 185.7,
    "dataPoints": 720
  },
  "timestamp": 1704067200000
}
```

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| peakLoad | float | 峰值负荷 (MW) |
| peakTime | string | 峰值时间 |
| valleyLoad | float | 谷值负荷 (MW) |
| valleyTime | string | 谷值时间 |
| avgLoad | float | 平均负荷 (MW) |
| loadRate | float | 负荷率 = 均值 / 峰值 |
| stdDeviation | float | 标准差 |
| dataPoints | int | 统计数据点数 |

---

## 4. 预测接口

### 4.1 获取最新预测

```
GET /api/v1/predict/forecast?horizon={hours}
```

**请求参数**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|:-----|:-----|:----:|:------:|:-----|
| horizon | int | ❌ | 24 | 预测时长（小时），最大 72 |

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "modelName": "LSTM",
    "modelVersion": "v1.0",
    "createdAt": "2024-01-01T00:00:00",
    "predictions": [
      {
        "time": "2024-01-01T01:00:00",
        "predictedLoad": 820.5,
        "lowerBound": 780.3,
        "upperBound": 860.7
      },
      {
        "time": "2024-01-01T02:00:00",
        "predictedLoad": 790.2,
        "lowerBound": 750.1,
        "upperBound": 830.4
      }
    ]
  },
  "timestamp": 1704067200000
}
```

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| modelName | string | 当前运行模型名称，主要为 LSTM；Prophet 仅兼容历史版本 |
| modelVersion | string | 模型版本号 |
| createdAt | string | 预测生成时间 |
| predictions | array | 预测数据点数组 |
| predictions[].time | string | 预测时间点 |
| predictions[].predictedLoad | float | 预测负荷值 (MW) |
| predictions[].lowerBound | float | 置信区间下界 |
| predictions[].upperBound | float | 运行参考区间上界 |
| forecastStartTime | string | 第一条预测对应的整点时间 |
| lowerBounds | array | 24 小时运行参考区间下界 |
| upperBounds | array | 24 小时运行参考区间上界 |
| intervalSource | string | 区间来源，`VALIDATION_RMSE_REFERENCE` 表示验证集 RMSE 参考区间 |
| modelVersionId | long | 当前运行模型版本 ID |
| futureWeatherAvailable | boolean | 是否取得未来天气缓存 |
| futureWeatherApplied | boolean | 当前模型是否实际使用未来天气 |
| futureWeatherFallback | boolean | 是否存在缺失天气并使用历史天气回退 |
| weatherSource | string | 未来天气来源，例如 `OPEN_METEO` |

---

### 4.2 预测精度统计（P1）

```
GET /api/v1/predict/accuracy?days={n}
```

**请求参数**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|:-----|:-----|:----:|:------:|:-----|
| days | int | ❌ | 7 | 统计最近 N 天 |

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "mape": 4.2,
    "rmse": 45.8,
    "mae": 38.5,
    "dailyAccuracy": [
      {
        "date": "2024-01-01",
        "mape": 3.8,
        "rmse": 42.1
      }
    ]
  },
  "timestamp": 1704067200000
}
```

---

### 4.3 数据质量摘要（P2）

```
GET /api/v1/predict/quality?start={start}&end={end}
```

不传时间范围时，按最新负荷数据向前统计 7 天。返回最新数据时间、数据延迟、小时点连续性、缺失点、天气字段缺失和数据来源分布。

### 4.4 线上预测复盘（P2）

```
GET /api/v1/predict/review?start={start}&end={end}
```

接口按每个预测目标时间保留最新一条预测，与 `load_data` 中同时间点实际负荷对齐，返回线上 MAPE、RMSE、MAE、峰值误差、覆盖率和 `series` 对比序列。少于 3 个已回填实际值时返回 `INSUFFICIENT_DATA`。

`/predict/forecast` 在模型版本存在验证集 RMSE 时，会基于 `1.2816 × RMSE` 生成上下参考边界，标记为 `VALIDATION_RMSE_REFERENCE`。该边界用于运行参考，不等同于严格概率置信区间。

历史输入中的温度、湿度来自 `load_data`；具备天气协变量的新 LSTM 还会接收未来 24 小时预报温度、湿度。旧 TorchScript 模型会忽略 `future_weather` 字段，接口通过 `futureWeatherApplied` 明确区分“已传递”和“已实际使用”。

### 4.5 未来天气预报（P2）

```
GET  /api/v1/weather/forecast?start={start}&end={end}
POST /api/v1/weather/sync
```

天气预报以快照方式保存到 `weather_forecast`，记录位置、预测时间、预报发布时间、温度、湿度和来源。预测调用会优先读取缓存，缓存缺少未来 24 小时点位时尝试刷新；刷新失败则继续使用历史天气回退。

Java 到 Flask 的请求会增加可选 `future_weather` 字段。重新训练 LSTM 会生成 `lstm_weather_scaler.pkl`，并在 `lstm_meta.pt` 写入 `future_weather_features`；模型管理在缺少该产物时会拒绝登记训练结果。

## 5. 告警接口

### 5.1 告警事件列表

```
GET /api/v1/alert/events?level={level}&start={time}&end={time}&page={page}&size={size}
```

**请求参数**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|:-----|:-----|:----:|:------:|:-----|
| level | string | ❌ | — | RED / ORANGE / YELLOW，不传则全部 |
| start | string | ❌ | 7 天前 | 起始时间 |
| end | string | ❌ | 当前 | 结束时间 |
| page | int | ❌ | 1 | 页码 |
| size | int | ❌ | 20 | 每页条数 |
| unreadOnly | bool | ❌ | false | 仅查未读 |

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "triggerTime": "2024-01-01T18:30:00",
        "level": "RED",
        "type": "THRESHOLD",
        "currentValue": 1320.5,
        "thresholdValue": 1200.0,
        "aiAnalysis": "⚠️ 紧急告警：当前负荷 1321MW，超出安全上限 1200MW 的 110%。建议立即关注负荷变化，必要时启动调峰预案。",
        "suggestion": "建议启动调峰预案，关注后续负荷变化趋势",
        "isRead": false,
        "resolvedAt": null
      }
    ],
    "total": 42,
    "page": 1,
    "size": 20,
    "pages": 3
  },
  "timestamp": 1704067200000
}
```

---

### 5.2 标记告警已读

```
PUT /api/v1/alert/events/{id}/read
```

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": null,
  "timestamp": 1704067200000
}
```

---

### 5.3 告警规则管理（P1）

```
GET    /api/v1/alert/rules          # 查询所有规则
POST   /api/v1/alert/rules          # 创建规则
PUT    /api/v1/alert/rules/{id}      # 更新规则
POST   /api/v1/alert/rules/{id}/snooze?minutes=30          # 暂挂规则
PUT    /api/v1/alert/rules/{id}/maintenance?enabled=true  # 维护模式
DELETE /api/v1/alert/rules/{id}      # 删除规则
```

**告警规则对象**：
```json
{
  "id": 1,
  "name": "夏季工作日高峰阈值",
  "type": "THRESHOLD",
  "config": {
    "threshold": 1200,
    "redRatio": 1.10,
    "orangeRatio": 1.00,
    "yellowRatio": 0.90,
    "coolingTime": 3600,
    "triggerDuration": 10,
    "hysteresis": 20,
    "maintenance": false
  },
  "isActive": true,
  "createdAt": "2024-01-01T00:00:00",
  "updatedAt": "2024-01-01T00:00:00"
}
```

`triggerDuration` 为连续超限触发时长（秒），`0` 表示立即触发；`hysteresis` 为恢复死区（MW），恢复条件为负荷低于黄色阈值减去该值。`maintenance=true` 或当前时间早于 `suspendUntil` 时不产生新告警。

### 5.4 告警运营指标（P1）

```
GET /api/v1/alert/events/metrics?start={ISO_DATE_TIME}&end={ISO_DATE_TIME}
```

### 5.5 告警呈现确认与端到端延迟（P0-05）

```http
POST /api/v1/alert/events/{id}/delivery-ack
Content-Type: application/json

{"clientRenderedAt":"2026-07-23T08:00:01","clientSessionId":"optional-session-id"}
```

已认证用户只能为自身写入确认。同一 `alertId + userId` 幂等，重复调用返回首条记录。该确认仅表示浏览器已完成告警首帧呈现，不改变已读、人工确认、恢复、工单解决或工单关闭状态。服务端以 `ackReceivedAt - sourceObservedAt` 计算延迟；缺失源观测时间的历史事件标记为 `LEGACY_EVIDENCE_MISSING`，不计入统计。

```http
GET /api/v1/alert/events/metrics?deliveryOnly=true&start={ISO_DATE_TIME}&end={ISO_DATE_TIME}&nodeId={nodeId}
```

返回 `deliverySamples`、`p50LatencyMs`、`p95LatencyMs`、`maxLatencyMs`、`excludedLegacyCount`、`excludedInvalidCount` 和统计时间范围。P50/P95 使用 nearest-rank：排序后取 `ceil(p * N)`；没有有效样本时延迟字段为 `null`，不是 `0`。

不传时间范围时默认统计最近 7 天，返回 `total`、各级别数量、`duplicateRate`、`averageAckMinutes`、`averageRecoveryMinutes` 等指标。

---

## 6. Agent 对话接口

### 6.1 自然语言对话（SSE 流式）

```
POST /api/v1/agent/chat
Content-Type: application/json
Accept: text/event-stream
```

**请求体**：
```json
{
  "message": "昨天最高负荷出现在几点？比前天高了多少？",
  "conversationId": "conv_abc123"
}
```

| 字段 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:-----|
| message | string | ✅ | 用户自然语言消息 |
| conversationId | string | ❌ | 会话 ID，不传则创建新会话 |

**SSE 事件流响应**：

```
Content-Type: text/event-stream
Cache-Control: no-cache
Connection: keep-alive
X-Accel-Buffering: no
```

**事件类型**：

| 事件 | 说明 | data 内容 |
|:-----|:-----|:----------|
| `thinking` | Agent 正在思考/查询 | `{"content": "正在查询昨日负荷数据..."}` |
| `text` | 文本回答片段 | `{"content": "昨日最高负荷出现在 18:30"}` |
| `chart` | ECharts 图表配置 | `{"config": {...}}` |
| `error` | 发生错误 | `{"message": "LLM 服务超时"}` |
| `done` | 流结束 | `{}` |

**完整 SSE 流示例**：
```

event: thinking
data: {"content":"正在查询昨日负荷数据..."}

event: text
data: {"content":"昨日最高负荷出现在 "}

event: text
data: {"content":"18:30，达到 1250MW，"}

event: text
data: {"content":"较前日最高值 1180MW 增长了 5.9%。"}

event: chart
data: {"config":{"xAxis":{"type":"time"},"series":[{"name":"昨日","data":[...]},{"name":"前日","data":[...]}]}}

event: done
data: {}

```

**错误 SSE 流示例**：
```

event: error
data: {"message":"LLM 服务调用超时，请稍后重试"}

event: done
data: {}

```

**对话历史**（P2 启用）：
```
GET /api/v1/agent/conversations              # 历史会话列表
GET /api/v1/agent/conversations/{id}         # 会话详情
DELETE /api/v1/agent/conversations/{id}      # 删除会话
```

---

## 7. WebSocket 推送

### 7.1 连接端点

```
ws://localhost:8080/ws/dashboard
```

使用 STOMP over SockJS 协议（`/ws/dashboard` 为 SockJS 端点）。

### 7.2 订阅主题

| 主题 | 说明 | 推送频率 |
|:-----|:-----|:---------|
| `/topic/load` | 实时负荷数据 | 每秒 |
| `/topic/predictions` | 预测结果更新 | 每小时 |
| `/topic/alerts` | 告警事件推送 | 事件触发 |
| `/topic/system` | 系统状态变更 | 事件触发 |

### 7.3 消息格式

**实时负荷推送** — `/topic/load`：
```json
{
  "type": "load_update",
  "data": {
    "time": "2024-01-01T10:00:00",
    "loadMw": 1100.5,
    "temperature": 12.3,
    "humidity": 58.0
  }
}
```

**告警推送** — `/topic/alerts`：
```json
{
  "type": "alert",
  "data": {
    "id": 42,
    "triggerTime": "2024-01-01T18:30:00",
    "level": "RED",
    "currentValue": 1320.5,
    "thresholdValue": 1200.0,
    "aiAnalysis": "⚠️ 紧急告警：当前负荷 1321MW..."
  }
}
```

**前端 STOMP 订阅示例**：
```typescript
const client = new Client({
  brokerURL: 'ws://localhost:8080/ws/dashboard',
  onConnect: () => {
    client.subscribe('/topic/load', (msg) => {
      const data = JSON.parse(msg.body);
      store.updateLoad(data.data);
    });
    client.subscribe('/topic/alerts', (msg) => {
      const data = JSON.parse(msg.body);
      store.addAlert(data.data);
    });
  }
});
client.activate();
```

---

## 8. 系统管理接口（P1）

### 8.1 健康检查

```
GET /api/v1/system/health
```

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "status": "UP",
    "components": {
      "db": "UP",
      "redis": "UP",
      "mlService": "UP"
    },
    "uptime": "72h 15m",
    "version": "1.0.0"
  },
  "timestamp": 1704067200000
}
```

### 8.2 模型版本管理

```
GET    /api/v1/model/versions                    # 模型版本列表
POST   /api/v1/model/versions/sync               # 扫描并登记 ml/models 下的本地模型产物
PUT    /api/v1/model/versions/{id}/activate      # 激活指定版本（也用于回滚）
POST   /api/v1/model/retrain                     # 手动触发 LSTM 后台重训练，body: {"modelName":"LSTM"}
GET    /api/v1/model/retrain/status              # 查询最近一次重训练任务状态
GET    /api/v1/model/retrain/history             # 查询最近 20 条训练记录
```

模型版本管理仅对 `OPERATOR` 和 `SYSTEM_ADMIN` 开放。`GET /model/versions` 是纯查询，不会登记或清理本地文件；`POST /model/versions/sync` 才负责扫描和登记本地模型产物。产品计划后续只使用 LSTM，当前页面和后端仍保留 Prophet 兼容入口，待后续移除页面按钮；Prophet 历史记录暂不删除。激活操作会在事务内将其他版本置为非激活，再将目标版本设为当前版本；当前系统的 Flask 推理服务仍按自身配置加载模型文件，版本切换和重训练完成都不会自动触发 Flask 热重载。

训练记录保存数据起止时间、样本数、训练耗时、输出尾部和产物清单。LSTM 训练成功前会检查模型权重、TorchScript、元信息、输入/目标 scaler 和特征列文件是否齐全。

---

## 9. 认证接口（P2）

### 9.1 登录

```
POST /api/v1/auth/login
```

**请求体**：
```json
{
  "username": "admin",
  "password": "******"
}
```

**成功响应** (200)：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "accessToken": "eyJhbGciOi...",
    "refreshToken": "eyJhbGciOi...",
    "expiresIn": 1800,
    "tokenType": "Bearer"
  },
  "timestamp": 1704067200000
}
```

### 9.2 刷新 Token

```
POST /api/v1/auth/refresh
```

**请求体**：
```json
{
  "refreshToken": "eyJhbGciOi..."
}
```

---

## 附录 A：接口交付检查清单

| 接口 | 方法 | 路径 | 优先级 | 状态 |
|:-----|:-----|:-----|:------:|:----:|
| 时间范围查询 | GET | `/api/v1/data/load` | P0 | ⬜ |
| 最新负荷 | GET | `/api/v1/data/load/latest` | P0 | ⬜ |
| 统计查询 | GET | `/api/v1/data/load/stats` | P0 | ⬜ |
| 预测查询 | GET | `/api/v1/predict/forecast` | P0 | ⬜ |
| 精度统计 | GET | `/api/v1/predict/accuracy` | P1 | ⬜ |
| 告警列表 | GET | `/api/v1/alert/events` | P0 | ⬜ |
| 标记已读 | PUT | `/api/v1/alert/events/{id}/read` | P0 | ⬜ |
| 告警规则 CRUD | */** | `/api/v1/alert/rules` | P1 | ⬜ |
| Agent 对话 | POST | `/api/v1/agent/chat` | P0 | ⬜ |
| 健康检查 | GET | `/api/v1/system/health` | P0 | ⬜ |
| 模型版本列表 | GET | `/api/v1/model/versions` | P1 | ✅ |
| 模型版本发布/回滚 | PUT | `/api/v1/model/versions/{id}/activate` | P1 | ✅ |
| 本地模型同步 | POST | `/api/v1/model/versions/sync` | P1 | ✅ |
| 模型重训练 | POST/GET | `/api/v1/model/retrain` | P1 | ✅ |
| 拓扑结构查询 | GET | `/api/v1/topology` | P1 | ✅ |
| 拓扑风险查询 | GET | `/api/v1/topology/risk` | P1 | ✅ |
| 故障场景推演 | POST | `/api/v1/topology/scenario` | P1 | ✅ |
| 登录 | POST | `/api/v1/auth/login` | P2 | ⬜ |
| 刷新 Token | POST | `/api/v1/auth/refresh` | P2 | ⬜ |
| WebSocket | — | `/ws/dashboard` | P0 | ⬜ |

---

> **文档审批**
>
> | 角色（Scrum） | 签字 | 日期 |
> |:-------------|:-----|:-----|
> | 项目经理（SM） | | |
> | 技术经理（Dev） | | |
> | 产品经理（PO） | | |
> | 测试工程师（QA） | | |

---

## 附录 B：当前实现同步补充（2026-07-19）

### B.1 Agent SSE 事件格式

`POST /api/v1/agent/chat` 当前使用 SSE 返回以下事件：

| 事件 | 数据格式 | 说明 |
|:-----|:---------|:-----|
| `thinking` | `{ "content": "正在查询..." }` | 中间状态，可出现多次 |
| `text` | `{ "content": "完整 Markdown 文本" }` | 一次性发送完整文本，保留换行 |
| `chart` | `{ "option": { ... } }` | ECharts option |
| `done` | `{ "conversationId": "...", "provenance": { ... } }` | 流结束，同时返回数据来源和模型可信度信息 |
| `error` | `{ "message": "..." }` | 异常信息 |

历史会话详情接口 `GET /api/v1/agent/conversations/{id}` 的消息对象补充字段：

```json
{
  "id": 1,
  "role": "assistant",
  "content": "## 分析结果\n...",
  "chartOption": "{\"xAxis\":...,\"series\":[...]}",
  "createdAt": "2026-07-19T10:00:00"
}
```

`chartOption` 为字符串化 JSON，前端解析后恢复图表。用户消息通常为 `null`。

### B.2 告警智能研判接口

```http
GET /api/v1/alert/events/{id}/judgement
POST /api/v1/alert/events/{id}/judgement
```

`GET` 只读取已经缓存的研判结果，不会因为查询而调用 LLM；没有缓存时返回空结果，前端提示调度员点击“重新研判”。只有显式 `POST` 才会重新调用 LLM，失败时降级为规则研判。

返回示例：

```json
{
  "alertId": 1001,
  "level": "RED",
  "currentLoad": 1240.0,
  "thresholdValue": 1100.0,
  "trendDirection": "RISING",
  "forecastPeakLoad": 1288.0,
  "hasExistingTicket": false,
  "hasOpenSimilarTicket": false,
  "shouldCreateTicket": true,
  "autoCreateTicket": false,
  "recommendedPriority": "URGENT",
  "decisionReason": "LLM 生成的研判原因",
  "dispatcherAdvice": "调度员处置建议",
  "operatorAdvice": "运维核查建议",
  "source": "LLM_AGENT",
  "createdAt": "2026-07-19T10:00:00"
}
```

说明：

- `source = LLM_AGENT` 表示详情研判真实调用 LLM。
- `source = RULE_BASED_FALLBACK` 表示 LLM 失败后使用规则降级。
- 红色告警只建议建单，`autoCreateTicket = false`，正式建单需要调度员确认。

### B.3 告警工单确认流程

```http
POST /api/v1/alerts/{alertId}/ticket
Content-Type: application/json

{
  "summary": "调度员确认后的工单概要"
}
```

前端红色告警弹窗中的“待确认工单草稿”不对应数据库草稿状态，只有调用上述接口成功后才创建正式 `alert_ticket` 记录。

> 当前接口行为和已完成/待移除能力以 [当前实现同步说明](./14-当前实现同步说明.md) 为准。
# P0-03 补充：固定提前量预测复盘

`GET /api/v1/predict/review` 保持原 URL，新增可选 `nodeId`、`leadHour`（仅 1/4/24，默认 24）和 `modelVersion`；`startTime`、`endTime` 为目标时刻范围（ISO-8601，Asia/Shanghai）。结果新增 `sampleCount`、`excludedSampleCount`、`wmape`、`peakTimeErrorHours`、峰值时刻与 MW、高负荷低估率及其 P90 阈值。历史 `forecast_run_id IS NULL` 数据不进入固定提前量统计。

## P0-04 实时遥测补充

秒级遥测由 `realtime_telemetry` 保存，且不写入小时级 `load_data`。当前来源为 `MOCK_REALTIME` 模拟数据。

`GET /api/v1/data/realtime/recent?minutes=30` 保持兼容，返回按 `observedAt` 升序的快照。`timestamp` 仍映射为 `observedAt` 的 epoch 毫秒；新增 `nodeId`、`observedAt`、`receivedAt`、`sourceInstanceId`、`qualityCode`、`qualityReason`、`dataSource`、`estimated`、`freshnessStatus`、`persistenceStatus`。

质量为 `GOOD`、`ESTIMATED`、`LATE`、`BAD`；`STALE` 是流新鲜度而不是历史点质量。`/topic/load` 推送同一字段，客户端按 `(dataSource, sourceInstanceId, sequence)` 去重，旧消息缺少新字段时以 `LEGACY_UNKNOWN` 兼容。`BAD` 点只审计持久化，不推送；`ESTIMATED` 和 `LATE` 可展示但不进入告警输入。

## P0-06 模型发布一致性

`PUT /api/v1/model/versions/{id}/activate` 接收可选 `X-Request-Id`，服务端校验数据库中的不可变版本目录和 artifact checksum，调用 Flask 内部 `POST /internal/models/reload`，随后用 `/health` 的 `modelVersion` 和 `artifactChecksum` 复核成功才更新数据库发布状态。内部接口仅由后端服务使用，要求环境变量提供的 `X-Internal-Token`；返回 `success`、`stage`、版本、校验和和加载时间。失败阶段为 `PATH_VALIDATION`、`MANIFEST_READ`、`CHECKSUM_VERIFY`、`MODEL_LOAD`、`SMOKE_INFERENCE`、`ATOMIC_SWAP`，不会暴露绝对路径或堆栈。
