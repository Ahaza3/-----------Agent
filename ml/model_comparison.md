# 电力负荷预测 — 模型对比报告

> 历史评估日期：2026-07-13 ｜ 数据：2 年逐小时模拟负荷（~17,520 条）
> 当前结论（2026-07-20）：产品已决定后续使用 LSTM 作为主模型并停用 Prophet；代码当前仍保留 Prophet 兼容能力，页面入口待下一步移除。

---

## 1. 候选模型

| 模型 | 类型 | 原理 |
|:-----|:-----|:-----|
| **Prophet** | 加法时间序列分解 | 趋势 + 周期（年/周/日）+ 节假日效应 + 外生变量 |
| **LSTM** | 深度学习序列模型 | 双层 LSTM 编码时序依赖，128 维隐层，滑动窗口 = 168h |

---

## 2. 评估指标

| 指标 | Prophet | LSTM | 说明 |
|:-----|:-------:|:----:|:-----|
| MAE (MW) | 36.61 | 27.24 | 平均绝对误差，越小越好 |
| RMSE (MW) | 45.22 | 37.46 | 对大误差惩罚更重 |
| **MAPE (%)** | **4.71** | **3.53** | 核心指标，LSTM 优 25% |
| 训练时间 | ~30s | ~90s (CPU, 29 epochs) | Prophet 更快 |
| 推理速度 | ~50ms | ~5ms (TorchScript) | LSTM 推理极快 |

> ✅ 实测数据，2026-07-13，数据：2 年逐小时模拟负荷（17,520 条），训练/测试 = 85/15

---

## 3. 模型特点对比

| 维度 | Prophet | LSTM |
|:-----|:--------|:-----|
| 可解释性 | ⭐⭐⭐⭐⭐ 趋势/周期/节假日分解可视化 | ⭐⭐ 黑盒模型 |
| 训练速度 | ⭐⭐⭐ 分钟级 | ⭐⭐ GPU 加速 |
| 小数据表现 | ⭐⭐⭐⭐ 几百条即可 | ⭐⭐ 需要充足数据 |
| 长序列依赖 | ⭐⭐⭐ 手动傅里叶阶数 | ⭐⭐⭐⭐⭐ 自动学习 |
| 外生变量 | ⭐⭐⭐⭐ 原生 add_regressor | ⭐⭐⭐ 需拼入特征 |
| 不确定性输出 | ⭐⭐⭐⭐⭐ 原生置信区间 | ⭐⭐ 需额外处理 |
| 工程部署 | ⭐⭐⭐⭐ pickle 加载简单 | ⭐⭐⭐ TorchScript 导出 |
| 调参门槛 | ⭐⭐⭐⭐ 参数语义清晰 | ⭐⭐ 需 DL 经验 |

---

## 4. 选型建议

### 历史 P0 阶段（快速交付）

**Prophet 作为基线**

- 优势：几分钟训练完成，天然输出 80% 置信区间，节假日处理开箱即用
- 适用场景：初期数据量少、需要快速上线验证业务
- 风险：MAPE 可能略高，极端天气下外推能力有限

### 当前阶段（精度优化）

**LSTM 作为主模型，Prophet 计划退出后续重训练流程**

- LSTM 目标 MAPE < 5%，捕获复杂非线性模式
- 训练前从 MySQL `load_data` 导出最新负荷和天气字段，生成 `featured_load_data.csv`
- 新训练的 LSTM 可使用未来 24 小时天气特征，天气不可用时回退历史天气
- 历史 Prophet 产物仍可被旧版本 Flask 逻辑识别；当前页面入口待清理

### 推荐策略

```
                    ┌─────────────────┐
                    │  Flask 推理服务  │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  LSTM 主模型     │ ← 默认推理
                    │  (MAPE < 5%)    │
                    └────────┬────────┘
                             │ 失败时 fallback
                    ┌────────▼────────┐
                    │ 历史 Prophet 产物 │ ← 兼容读取
                    │  不作为训练入口  │
                    └─────────────────┘
```

---

## 5. 改善方向（后续 Sprint）

| 方向 | 描述 | 预期收益 |
|:-----|:-----|:--------:|
| 模型清理 | 确认无历史依赖后移除 Prophet 训练脚本和依赖 | 降低维护成本 |
| 注意力机制 | Transformer / Attention LSTM | 长周期依赖改善 |
| 多步预测 | Seq2Seq 直接输出 24h | 避免误差累积 |
| 在线学习 | 新数据到达后增量微调 | 适应概念漂移 |
| 概率预测 | Quantile Regression / DeepAR | 分位数预测替代点预测 |
